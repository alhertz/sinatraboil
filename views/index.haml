!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "UTF-8"}/
    %title
    / These should include all the JS files defined in the app.
    = js  :main
    = css :main
  %body
    %h1 Hello!
    %h4 View the source of this file to see the JS files as they were included.
    %h3 Image test:
    / This should have width and height.
    = img '/images/icon.png'
    %h3 Image embedding test:
    #image_embedding_test
      / This should have a CSS background that uses a data: URI.
      / Inspect that <div> to see.
